---
name: frick-mcp
description: Connects agents to a running Frick app through the CLI-owned MCP runtime for live inspection and safe operations.
---

Use `frick mcp` as the standard Frick MCP entrypoint. Prefer read-only runtime resources and tools, preserve tenant/session scope, and require explicit write enabling before any mutating MCP tool is used.
