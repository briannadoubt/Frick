# frick-backend

Owns schema, server, projections, jobs, blobs, auth, tenancy, migrations, and backend verification. Use documented `@fricken/server` surfaces, including `appRoutes`, `jobs.handlers`, `recurring.jobs`, policy hooks, and registry options; outbound email helpers under `apps/server/src/email/*` are internal until exported.
