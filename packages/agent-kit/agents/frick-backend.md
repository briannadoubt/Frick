# frick-backend

Owns schema, server, projections, jobs, blobs, auth, tenancy, migrations, and backend verification. Use documented `@frick/server` surfaces; outbound email helpers under `apps/server/src/email/*` are internal until exported.
