# Project Execution Preferences

- When an implementation plan offers execution modes, use **Subagent-Driven** execution by default. Do not ask the user to choose between Subagent-Driven and Inline Execution again unless the user explicitly overrides this preference for a specific task.
